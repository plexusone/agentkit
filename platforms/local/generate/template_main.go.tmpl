// Code generated by agentkit generate. DO NOT EDIT.
// Source: {{.SpecDir}}
// Generated: {{.GeneratedAt}}

package main

import (
	"os"

	"github.com/spf13/cobra"
)

const (
	programName    = "{{.ProgramName}}"
	programVersion = "{{.Version}}"
	teamName       = "{{.TeamName}}"
)

// Global flags
var (
	flagProvider  string
	flagModel     string
	flagWorkspace string
	flagVerbose   bool
	flagTimeout   string
)

func main() {
	if err := rootCmd.Execute(); err != nil {
		os.Exit(1)
	}
}

var rootCmd = &cobra.Command{
	Use:     programName,
	Short:   "{{.TeamDescription}}",
	Long:    `{{.TeamDescription}}

Generated from multi-agent-spec by agentkit.`,
	Version: programVersion,
}

func init() {
	// Global flags
	rootCmd.PersistentFlags().StringVar(&flagProvider, "provider", "{{.DefaultProvider}}", "LLM provider (anthropic, openai, gemini, xai, ollama)")
	rootCmd.PersistentFlags().StringVar(&flagModel, "model", "{{.DefaultModel}}", "Model name or tier (haiku, sonnet, opus)")
	rootCmd.PersistentFlags().StringVar(&flagWorkspace, "workspace", ".", "Workspace directory")
	rootCmd.PersistentFlags().BoolVarP(&flagVerbose, "verbose", "v", false, "Verbose output")
	rootCmd.PersistentFlags().StringVar(&flagTimeout, "timeout", "10m", "Execution timeout")

	// Add subcommands
	rootCmd.AddCommand(runCmd)
	rootCmd.AddCommand(resumeCmd)
	rootCmd.AddCommand(agentCmd)
	rootCmd.AddCommand(prereqsCmd)
	rootCmd.AddCommand(credsCmd)
	rootCmd.AddCommand(describeCmd)
	rootCmd.AddCommand(respondCmd)
}
